# -*- mode: python -*-

block_cipher = None


a = Analysis(['main_app.py'],
             pathex=['C:\\Users\\Edward\\Desktop\\Develop\\Projects\\KGTU_DB'],
             binaries=None,
             datas=[('DataBase'), ('Resources')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='main_app',
          debug=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='main_app')
